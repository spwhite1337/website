# backend

## Procedure

- Python 3.5
- `pip install -e .`
- `aws configure`
    - Enter keys from `s3://scott-p-white` bucket
- `python download.py`

## Unittests

- `cd tests`
- `python -m unittest`

Output: